[build]
public_url="/dist/"

# [serve]
# # The address to serve on LAN.
# address = "127.0.0.1"
# # The address to serve on WAN.
# # address = "0.0.0.0"
# # The port to serve on.
# port = 8081

[[hooks]]
stage = "post_build"
command = "sh"
command_arguments = ["-c", "npx tailwindcss -i ./styles/tailwind.css -o ./dist/.stage/css/index.css"]